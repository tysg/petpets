setup:
    addons:
        - plan: heroku-postgresql
          as: DATABASE
build:
    docker:
        web: Dockerfile.prod

release:
    image: web
    command:
        - heroku pg:reset DATABASE
        - yarn ts-node sql/sqlSetup.ts
